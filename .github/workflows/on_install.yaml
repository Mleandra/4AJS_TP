name: "install"
on:
  push
jobs:
  install_jobs:
    runs-on: ubuntu-latest
    steps:
      - name: "install job"

        run: |
         ls -la
         tree
        working-directory: ../../





